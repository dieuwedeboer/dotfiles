[Service]
# Same file as apply-font-size reads when the vars are not already in the env.
# emacs.service starts at default.target, before fish, so local.fish never applies.
EnvironmentFile=-%h/.config/environment.d/monarchy.conf
