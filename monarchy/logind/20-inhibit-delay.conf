# Copied from Omarchy. omarchy-settings would have installed this; we do not
# install that package. A delay inhibitor is a timer: logind suspends when
# the window expires whether or not the session is locked. Five seconds is
# not enough when a lid close also reconfigures displays.
[Login]
InhibitDelayMaxSec=15
