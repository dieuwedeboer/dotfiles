# What Monarchy tracks. This used to pin a git commit on a third-party ZFS
# fork; it now names the official package and channel, and pacman does the
# pinning. Deployed to /etc/omarchy.lock at apply.
package=omarchy
channel=stable
